# https://www.debian.org/doc/manuals/apt-guide/index.pl.html
# http://httpbin.org/ip
# https://olivermarshall.net/how-to-upload-a-file-to-google-drive-from-the-command-line/

# --- PRZYDATNE COMENDY
name -a
free
nproc             
passwd
clear
reboot        
lsb_release -a      	#distribution info
nmap
su                  	#user switch
ufw 			#firewall
htop  			#good process list
screen 			# multiple screens / sessions
watch df -h 
cat nazwa.pliku 	#podagląd pliku w konsoli
touch / mkdir / rm -r / 

#VPS settings
hostname add.your.domain

#BASIC INSTALL COMPONENTS AND PROGRAMS
sudo apt install screen && sudo apt install wget
sudo apt install ranger && sudo apt install htop
sudo apt install docker.io && sudo apt install docker-compose
yes | sudo apt install fish	#sudo apt install fish --yes https://fishshell.com/docs/3.0/tutorial.html

#Essential security measures:
- use regular user
- enable firewall
- add SSH key
